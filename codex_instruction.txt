'Fix the ESMFold one_hot index tensor failure. The error is one_hot index tensor failure on cpu when running physics validation. First, check if the existing _apply_openfold_onehot_compat_patch() function is being called properly. Add debug logging to see tensor dtypes. Enhance the patch to handle all tensors that might need .long() conversion. Add a graceful fallback if ESMFold prediction fails.'  
